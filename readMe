
This is a WinForm program, it was designed to contol the JAI camera.

the Jai_FactoryDotNET.dll, log4net.dll, log4net is needed.

this project used OpenCV to develop vision algorithm by C++ DLL, and use C# to do GUI, it has a excellent result.
